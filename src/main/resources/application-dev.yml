spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/drogueria_bellavista
    username: postgres
    password: JUNIORDIAZ
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

  # Configuración de email para desarrollo (REEMPLAZA CON TUS DATOS)
  mail:
    host: smtp.gmail.com
    port: 587
    username: invetoryrx@gmail.com  # ← CAMBIA ESTO
    password: zkck bmav klok qbum    # ← CAMBIA ESTO
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  mail:
    from: invetoryrx@gmail.com  # ← ACTUALIZADO
  frontend:
    url: http://localhost:5173
  stripe:
    api:
      # ⚠️ CONFIGURACIÓN DE STRIPE - Reemplazar con tus credenciales reales
      # Obtén tus keys en: https://dashboard.stripe.com/apikeys
      key: ${STRIPE_SECRET_KEY:sk_test_placeholder_development_only}
      public-key: ${STRIPE_PUBLIC_KEY:pk_test_placeholder_development_only}

logging:
  level:
    root: INFO
    com.drogueria.bellavista: DEBUG
    org.springframework.mail: DEBUG  # ← Para ver logs de email